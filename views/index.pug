extends layout

block content
  h1= title
  p Welcome to #{title}
  //- a(class='button') Press Me
  <button (type="button",class="btn btn-secondary"onclick="update()")>PressMe</button>
  br
  br
  #target Display 
  script.
    var client_count = 0;
    function update()
    {
      client_count = client_count + 1;
      document.getElementById("target").innerHTML = "Value now "+client_count;  
    }
    br
    a(href='season') Go to season Page